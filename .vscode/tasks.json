{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "cmd",
            "args": [
                "/c",
                "npm run compile"
            ],
            "problemMatcher": [
                "$tsc"
            ],
            "label": "npm: compile",
            "detail": "tsc -p ./"
        }
    ]
}
